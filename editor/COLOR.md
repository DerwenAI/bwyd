Find a "Group Complement" to balance a color palette for a dish: rather than complementing each color individually,
 - Average the three hues (using arc-aware averaging to handle the 360° wraparound)
 - Rotate that averaged hue by 180°
 - Generate 2–3 swatches around that complementary midpoint, using the inputs' average saturation/lightness as a guide

This gives a coherent opposing "zone" rather than three scattered complements.
